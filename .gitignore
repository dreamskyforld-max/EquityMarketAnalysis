# ── 敏感配置（不传 GitHub）──
config.conf
db_config.json
ticker_config.json
.env

# ── 虚拟环境 ──
.venv/
__pycache__/
.idea/
.coverage

# ── 部署 / 系统服务文件 ──
system/

# ── 日志 ──
*.log

# ── 数据与缓存产物 ──
benchmark_data.txt
excess_return_data.txt
quote_data.txt
money_flow_data.txt
trend_cache/*.json
trend_cache/*.txt
south_cache_*.json
*.csv

# ── 开发临时脚本（不传 GitHub）──
test.py
test_ticker.py

# ── AI 生成的 Markdown 文档（不传 GitHub）──
*.md

# ── 测试缓存 ──
.pytest_cache/
