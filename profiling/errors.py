#!/usr/bin/env python3
"""画像层异常。

单独成模块只为避开循环依赖：quantile（底层工具）与 tags（标签实现）都需要抛/捕
SkipTag，若定义在任一侧都会被另一侧反向导入。
"""
from __future__ import annotations


class SkipTag(Exception):
    """标签主动弃权：本次不产出任何结果。

    语义边界（关键）
    ---------------
        raise SkipTag      = 「这次没法算」→ 引擎不写库、不 diff，现有版本原样保留
        返回空 DataFrame   = 「算出来是没有」→ 是真实结果，正常参与 diff（缺失即关闭）

    这两者绝不能混。2026-09-15 事故正是把「上游断供（没法算）」表达成了「结果集为空
    （标签不成立）」，导致单日关闭 55,035 行历史版本。

    本异常是**机制**而非策略：要不要弃权、按什么标准弃权，完全由每个标签自己决定
    （各标签依赖的源与时效容忍度本就不同）。引擎不做任何统一的数据健康检查——
    通用检查由运维平台负责（数据源 / 中间表 / 结果表多维度）。
    """
