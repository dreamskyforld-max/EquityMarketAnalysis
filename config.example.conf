# 项目配置文件模板 —— 复制为 config.conf 并填写真实值
# 注意：config.conf 不传 GitHub（见 .gitignore）

[database]
host = localhost
port = 5432
dbname = market_db
user = YOUR_DB_USER
password = YOUR_DB_PASSWORD

[futu]
host = 127.0.0.1
port = 11111

[ticker]
stocks = HK.00700,SH.600900,SH.520900,SH.600941,HK.09660
# QUOTE(LV1 实时报价) 订阅标的：富途免费 LV1 额度有限，只能显式指定少量有权限代码。
# 留空则不订阅 QUOTE（逐笔主链路不受影响）。
quote_stocks = HK.00700
buffer_size = 500
flush_interval_seconds = 10
reconnect_delay_seconds = 30

[eastmoney]
# 港股主板板块代码（clist 接口 fs 参数），默认 m:128 t:3，需实测确认
hk_fs = m:128 t:3
page_size = 200
timeout = 15
max_retry = 3

[fred]
api_key = YOUR_FRED_API_KEY

[wecom]
bot_a_id = YOUR_WECOM_BOT_A_ID
bot_a_secret = YOUR_WECOM_BOT_A_SECRET
bot_b_id = YOUR_WECOM_BOT_B_ID
bot_b_secret = YOUR_WECOM_BOT_B_SECRET

[wecom_webhook]
key = YOUR_WECOM_WEBHOOK_KEY

[sync_server]
host = YOUR_SYNC_SERVER_HOST
user = root
password = YOUR_SYNC_SERVER_PASSWORD
